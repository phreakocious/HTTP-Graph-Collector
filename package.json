{
  "name": "http-graph-collector",
  "version": "0.4.0",
  "private": true,
  "description": "Chrome extension to collect HTTP request/response data for Gephi",
  "scripts": {
    "lint": "eslint dist/",
    "test": "node --test 'test/**/*.test.mjs'",
    "check": "npm audit && npm run lint && npm test"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-security": "^3.0.0",
    "globals": "^16.0.0"
  }
}
