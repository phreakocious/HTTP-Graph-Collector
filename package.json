{
  "name": "http-graph-collector",
  "private": true,
  "license": "MIT",
  "description": "Chrome extension that graphs your HTTP traffic — live in the browser, via the bundled Python tools, or in Gephi",
  "scripts": {
    "lint": "eslint dist/ viewer/",
    "test": "node --test 'test/**/*.test.mjs'",
    "check": "npm audit && npm run lint && npm test"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-security": "^3.0.0",
    "globals": "^16.0.0"
  }
}
